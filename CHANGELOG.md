# 更新日志

格式参照 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/)，版本号遵循 [语义化版本](https://semver.org/lang/zh-CN/)。

## [1.0.0] - 2026-09-24

第一个正式版本。

### 新增

- **体检**：一键检查 Node.js、pnpm、dsh、下载源、DSH_HOME、工作区、端口、启动配置、待决的构建脚本和插件兼容性，问题项直接跳到对应页面处理，报告可复制到 issue。pnpm 用插件安装时完全相同的环境来检查，环境变量类问题会在这里先暴露。
- **下载源测速**：在设置里实测各下载源的响应时间并标出最快的一个（绕过本地 HTTP 缓存，测的是真实网络）。
- **插件列表导出 / 导入**：把一个配置的插件存成文件，换电脑或重装后一次装回；也能直接读另一台电脑 dsh 配置目录里的 `package.json`。官方插件按本机的 dsh 版本重新锁定，本地路径会被跳过并说明原因。
- **启动器更新提示**：从 GitHub Releases 检查新版本，侧边栏和「设置 → 关于」给出下载入口。
- **开机自动运行**（静默启动到托盘）与**打开启动器时自动启动 dsh**。免安装版的开机项指向真实的 exe，而不是它运行时解压出的临时副本。

## [0.1.5] - 2026-09-23

### 改进

- 同一配置下的插件安装合并成一次 pnpm 运行：每次运行都要做一次供应链校验（依赖多时约 30 秒），连装多个插件不再成倍变慢。
- 安装进度显示当前阶段（校验供应链 / 解析依赖 / 下载 / 变更包数）；下载源多次超时时提示换源。
- pnpm 拦截构建脚本时，除了「允许构建」还可以选「不运行」——两者都能解除 pnpm 11 对整个配置的安装阻塞。

### 修复

- 旧的待决构建脚本不再被误报成之后每次安装失败的原因；只以本次运行的输出为准。

## [0.1.4] - 2026-09-23

### 修复

- 插件市场找不到没有声明 keywords 的插件（例如 `@linxin666/dsh-web-ui-all`）：搜索时额外做一次不限关键字的 npm 查询。
- 英文短词改为按词边界匹配，`all` 不再命中 "wallpaper"、`ui` 不再命中 "build"。

### 新增

- 搜索无结果或结果不含所查包名时，说明原因：npm 上没有这个包、它不是 dsh 插件，或只有同名的其他作者的 scoped 版本。
- 安装前的预览显示作者声明的迁移（`dsh.migrate`），提示改装新包。

## [0.1.3] - 2026-09-23

### 修复

- 选择「不使用代理」时所有插件都安装失败（pnpm 报 `Invalid URL`）：此前代理变量被设成空字符串，现在改为删除。
- 安装失败时优先显示 pnpm 自己的 `[ERROR]` 信息，而不是 dsh 的通用包装行。

## [0.1.2] - 2026-09-22

### 改进

- 插件市场改为本地索引 + 自己排序：npm 搜索接口的质量分全部为 1.0，中文查询几乎排不出东西。支持中文查询、精确包名直查、综合 / 下载量 / 最近更新排序、只看组合包、隐藏不兼容，以及按话题浏览。

## [0.1.1] - 2026-09-20

### 修复

- 启动失败时显示的是 Node 回显的源码行而不是真正的错误信息。

### 新增

- dsh 因某个插件启动失败时，识别出该插件，一键「停用该插件并重启」。

## [0.1.0] - 2026-09-19

首个可用版本：自动下载 Node.js 与 pnpm、安装与切换 dsh 版本、启动和守护 `dsh web`、插件安装与管理、插件市场、安装包与免安装版。

[1.0.0]: https://github.com/zhw000/deepseek-harness-launcher/releases/tag/v1.0.0
